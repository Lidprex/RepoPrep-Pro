# RepoPrep Pro v2 — Lidprex Labs
# No external dependencies required.
# Uses Python standard library only (tkinter is bundled with Python).
#
# Python 3.8+ required
#
# ─── Optional: build to .exe ───────────────────────────────────────
# pip install pyinstaller
# Then run:  build.bat   (Windows)
#       or:  build.sh    (macOS / Linux)
